# PR Reviewer Assignment Service

Микросервис для автоматического назначения ревьюеров на Pull Request'ы.
Реализован в рамках тестового задания на позицию Backend Developer (Go).

📄 **Спецификация API:** [openapi.yaml](./openapi.yaml)

## 🚀 Быстрый старт

Сервис полностью контейнеризирован. Для запуска требуются **Docker** и **Docker Compose**.

### Запуск приложения
```bash
make run
# или: docker-compose up --build
```

Сервис будет доступен по адресу: `http://localhost:8080`

### Остановка
```bash
make stop
```

## 🛠 Команды (Makefile)

| Команда | Описание |
| :--- | :--- |
| `make build` | Сборка сервиса (в контейнерe) |
| `make bd` | Запуск БД (в контейнере) |
| `make up` | Запуск сервиса и БД (в контейнерах) |
| `make run` | Сборка и запуск сервиса и БД (в контейнерах) |
| `make deps` | Установка зависимостей для локального использования (goose) |
| `make test` | Запуск тестов в изолированном окружении (требуется локальный Go) |
| `make load-test` | Запуск нагрузочного тестирования (k6) |
| `make lint` | Запуск линтера (golangci-lint) |
| `make stop` | Остановка контейнеров |
| `make clean` | Остановка контейнеров и удаление данных БД |

## 🧪 Тестирование

В проекте реализованы **Интеграционные** и **E2E** тесты, проверяющие работу API, бизнес-логики и базы данных в связке.

Для обеспечения чистоты данных тесты запускаются в **изолированном окружении**:
1.  Автоматически поднимается отдельный Docker-контейнер `db_test` с Postgres (порт 5433).
2.  Накатываются миграции `goose` в БД `reviewer_db_test`.
3.  Прогоняются тесты (`go test ./...`).
4.  Контейнер останавливается.

```bash
make deps

make test
```

### Нагрузочное тестирование
Проведено тестирование производительности с использованием **k6**.
*   **Результат:** Сервис выдерживает **67+ RPS** с временем ответа **p95 < 120ms** (требование ТЗ: 5 RPS, < 300ms).
*   Подробный отчет: [LOAD_TEST.md](./LOAD_TEST.md)

Для воспроизведения теста:
```bash
make run
# Внимание: нагрузочные тесты запускаются против основного инстанса приложения и основной БД
make load-test
```

## 🛠 Стек технологий

*   **Язык:** Go 1.25.
*   **Архитектура:** Clean Architecture (Transport -> Service -> Repository).
*   **База данных:** PostgreSQL 15.
*   **Маршрутизация:** [`go-chi/chi`](https://github.com/go-chi/chi) (идиоматичный и легковесный роутер).
*   **Миграции:** [`goose`](https://github.com/pressly/goose) (накатываются автоматически при старте контейнера).
*   **Линтер:** [`golangci-lint`](https://github.com/golangci/golangci-lint) (строгая конфигурация).

## 💡 Реализация и Архитектурные решения

### 1. Clean Architecture & Dependency Injection
Сервис разделен на слои с четкими границами ответственности:
*   **Handler (Transport):** Валидация JSON, маппинг ошибок сервиса в HTTP-коды (400/404/409). Не зависит от базы данных.
*   **Service (Business Logic):** Реализация алгоритмов (выбор ревьюера, транзакции). Зависит от интерфейсов репозиториев (`deps.go`), а не от конкретной реализации.
*   **Repository (Storage):** Реализация работы с PostgreSQL.

### 2. Надежность и Консистентность
*   **Транзакции:** Операции, затрагивающие несколько сущностей (создание PR, переназначение, деактивация команды), выполняются атомарно.
*   **Upsert:** Создание команды (`/team/add`) реализовано через `ON CONFLICT DO UPDATE`, что гарантирует идемпотентность.
*   **Concurrency:** Использование `mutex` не потребовалось, так как консистентность гарантируется ACID-свойствами БД.

### 3. Алгоритм выбора ревьюеров
*   Используется `math/rand` (Shuffle) для случайного выбора кандидатов.
*   Исключаются: автор PR, уже назначенные ревьюеры, неактивные пользователи.

### 4. DevOps и Observability
*   **Graceful Shutdown:** Реализован корректный процесс завершения работы сервера с закрытием соединений.
*   **Health & Stats:** Добавлен эндпоинт `/health/stats` для мониторинга.
*   **Docker:** Multi-stage сборка для минимизации размера образа (Alpine).

## ⚖️ Компромиссы и Tech Debt

В рамках задания были приняты следующие архитектурные упрощения (Pragmatic approach):

1.  **Domain Models vs DTO:**
    *   Доменные структуры (`internal/domain`) содержат JSON-теги и используются в транспортном слое. В крупном Enterprise-проекте стоило бы разделить их на DTO и Domain Models с маппингом, но здесь это привело бы к дублированию кода без явной пользы.

2.  **Стратегия тестирования:**
    *   Сделан упор на **Интеграционные тесты** с реальной БД (через `docker-compose` или локальный Postgres). Это дает 100% уверенность в работоспособности SQL-запросов и транзакций, что критично для данного сервиса. Юнит-тесты с моками (mockery) возможны (благодаря интерфейсам), но в рамках тестового задания приоритет отдан реальным сценариям.

3.  **Конфигурация:**
    *   Параметры (DSN, порт) читаются из ENV с дефолтными значениями для удобства локального запуска. В продакшене рекомендуется использовать конфигурационные файлы (yaml/json) или Secret Manager.

4. **Testing Environment:**
    *   Тесты запускаются локально (`go test`) для скорости разработки. В CI/CD пайплайне тесты должны запускаться внутри Docker-контейнера для обеспечения полной изоляции и идентичности окружения с продакшеном.

5. **Load Testing Environment:**
    *   Нагрузочные тесты (`k6`) запускаются против основного инстанса приложения и основной БД для упрощения локального запуска и экономии ресурсов машины. В реальном проекте нагрузочное тестирование должно проводиться на изолированном **Staging/Performance окружении** с отдельной базой данных, чтобы не засорять Development-окружение.
