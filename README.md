# PR Reviewer Assignment Service

Сервис для автоматического назначения ревьюеров на Pull Request'ы.
Выполнено в рамках тестового задания (Осень 2025).

## 🚀 Быстрый старт

Для запуска требуется только **Docker** и **Docker Compose**.

```bash
# Запуск сервиса и базы данных
make run
# или docker-compose up --build
```

Сервис будет доступен по адресу: `http://localhost:8080`

Чтобы остановить:
```bash
make stop
```

Для запуска интеграционных тестов (требуется локальный Go):
```bash
make test
```

## 🛠 Стек технологий

*   **Язык:** Go 1.25
*   **База данных:** PostgreSQL 15
*   **Роутер:** go-chi (выбран за легковесность и идиоматичность)
*   **Миграции:** goose
*   **Архитектура:** Clean Architecture (Handler -> Service -> Storage)

## 💡 Принятые решения и допущения

В ходе выполнения задания были приняты следующие архитектурные решения:

1.  **Алгоритм выбора ревьюеров:**
    *   Используется генератор псевдослучайных чисел `math/rand`. В Go 1.20+ он инициализируется автоматически (deprecated `rand.Seed`), что гарантирует случайность выборки при каждом запуске сервиса.
    *   Выбор происходит в рамках одной транзакции с созданием PR, чтобы гарантировать целостность данных.

2.  **API vs Database:**
    *   В базе данных используется нормализованная схема (связь User -> Team через Foreign Key).
    *   В API возвращаются вложенные структуры (Team включает массив Members), как того требует спецификация OpenAPI. Сборка данных происходит на уровне Service.

3.  **Обработка статусов (Merge):**
    *   Операция `/pullRequest/merge` реализована идемпотентно. Повторный вызов для уже смерженного PR возвращает успех (200 OK) и актуальное состояние.

4.  **Работа с временем:**
    *   Поля `created_at` и `merged_at` заполняются базой данных (Postgres), но при чтении маппятся в Go-структуры.

5.  **Конфигурация:**
    *   Все параметры (DSN базы, порт) вынесены в переменные окружения, но имеют дефолтные значения для удобства локального запуска.